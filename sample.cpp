#include<bits/stdc++.h>
using namespace std;

void return_pair(vector<int> v, int N){

}

    int main()
{
    vector<int> v;
    int n;
    cin >> n;
    int temp;
    for (int i = 0; i < n; ++i){
        cin >> temp;
        v.push_back(temp);
    }

    int N;
    cin >> N;
    return_pair(v, N);
}