#include <bits/stdc++.h>
using namespace std;

int main()
{
    string str, str2;
    str2 = "Hi";
    str = "Hi Hello";
    // str2 = "";
    // str2.clear();
    // str.erase(0, 3);
    // cout << str;
    // cout << str.find("Hel");

    // for (int i = 0; i < str.size(); ++i){
    //     str2.push_back(str[i]);
    // }

    // str.insert(2, str2);
    string st = "111";
    int i = stoi(st);
    cout << i + 2;
}